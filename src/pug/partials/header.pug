mixin header()
  header.container
    ul.header-navigate
      each item in headerList
        li.header-navigate__item
          a(href="#!").header-navigate__link= item
        span.cross -
    .header-search
      input(type="text" placeholder="Tìm kiếm ở đây ...")
      i.fa-solid.fa-magnifying-glass.icon
